apiVersion: 1

datasources:
  - name: grafana-postgresql-datasource
    type: postgres
    url: postgres:5432
    database: envobsDB
    user: user
    secureJsonData:
      password: "password"
    jsonData:
      sslmode: "disable"
      postgresVersion: 1700
      timescaledb: false
